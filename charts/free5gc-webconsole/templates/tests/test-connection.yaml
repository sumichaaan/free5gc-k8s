apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free5gc-webconsole.fullname" . }}-test-connection"
  labels:
    {{- include "free5gc-webconsole.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "free5gc-webconsole.fullname" . }}:{{ .Values.services.console.port }}']
  restartPolicy: Never
