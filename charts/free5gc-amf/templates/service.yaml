apiVersion: v1
kind: Service
metadata:
  name: {{ include "free5gc-amf.fullname" . }}
  labels:
    {{- include "free5gc-amf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "free5gc-amf.selectorLabels" . | nindent 4 }}
