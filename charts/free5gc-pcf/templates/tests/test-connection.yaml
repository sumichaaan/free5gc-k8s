apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free5gc-pcf.fullname" . }}-test-connection"
  labels:
    {{- include "free5gc-pcf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "free5gc-pcf.fullname" . }}:{{ .Values.services.sbi.port }}']
  restartPolicy: Never
