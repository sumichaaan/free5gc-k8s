apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free5gc-udm.fullname" . }}-test-connection"
  labels:
    {{- include "free5gc-udm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "free5gc-udm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
