apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free5gc-nssf.fullname" . }}-test-connection"
  labels:
    {{- include "free5gc-nssf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "free5gc-nssf.fullname" . }}:{{ .Values.services.sbi.port }}']
  restartPolicy: Never
